- A single A100 cannot train 1B parameters from scratch. You need data parallelism + model parallelism across multiple GPUs.

- Multi-GPU communication for distributed training 
- A 1B model trained on 100B tokens is better than a 1B model on 10B tokens. Data scaling matters more than model size at this scale 

- This model will be a decoder-only transformer (Like GPT,Llama,Mistral) 
- Core framework is pytorch 
- Distributed training => Pytorch FSDP
- Attention optimization => FlashAttention-2 
- Data loading => Hugging face 
- Tokenization  => SentencePiece
- Experiment tracking => Weights and Biases, TensorBoard 
- Checkpointing => HugginFace Transformers format 
- Parameters" are essentially the "knowledge" of the model. They are the variables (weights and biases) that the model learns and adjusts during training to make accurate predictions . 
- 1million is about the 4mb of memory 
- 5million is about the 20mb of memory 
- `pip install datasets`
- 

- Once the dataset is ready the next goal is to build the tokenizer . 
- The tokenizer converts the text into numbers that the model understands . 
- For the python code we use BYTE LEVEL i.e BPE -> It learn to merge common dcharacter sequence into tokens .
- 